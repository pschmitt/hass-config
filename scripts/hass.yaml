  ha_restart:
    alias: 'Restart HASS'
    sequence:
      - service: shell_command.ha_restart

  ha_reload_config:
    alias: 'Reload core config'
    sequence:
      - service: homeassistant.reload_core_config
